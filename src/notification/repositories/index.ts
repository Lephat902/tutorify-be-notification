import { NotificationRepository } from './notification.repository';
import { UserRepository } from './user.repository';

export const Repositories = [
    NotificationRepository,
    UserRepository,
];

export { NotificationRepository, UserRepository };